{% if content | toc %}
<div class="terminal">
  <p>
    <button class="btn host"><span>table of contents</span></button>
    <button class="{{'btn dir2' if tag != 'home' else 'btn dir'}}">
      <span>~{% if tag != 'home' %}/{{ dir }}{% endif %}</span>
    </button>
  </p>
  {{ content | toc | safe }}
</div>
{% endif %}
